import { Ionicons } from "@expo/vector-icons"

export const ICONS = {
  backArrow: "arrow-back" as keyof typeof Ionicons.glyphMap,
  search: "search" as keyof typeof Ionicons.glyphMap,
  delete: "trash" as keyof typeof Ionicons.glyphMap,
  camera: "camera" as keyof typeof Ionicons.glyphMap,
  calendar: "calendar-sharp" as keyof typeof Ionicons.glyphMap
}